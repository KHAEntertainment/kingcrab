{
  "_comment": "OpenClaw Plugin Configuration for KingCrab",
  "_comment_more": "This should be merged into your ~/.openclaw/openclaw.json file",

  "plugins": {
    "entries": {
      "@khentertainment/kingcrab-plugin": {
        "_enabled_comment": "Enable or disable the KingCrab plugin",
        "enabled": true,

        "_config_comment": "Plugin-specific configuration",
        "config": {
          "_daemonUrl_comment": "URL of the KingCrab daemon HTTP API",
          "_use_unix_comment": "For production, use Unix socket: unix:///var/run/kingcrab.sock or /var/run/kingcrab.sock",
          "daemonUrl": "http://localhost:8080",

          "_timeout_comment": "Request timeout in milliseconds",
          "timeout": 30000,

          "_commands_comment": "Default allowed commands for this plugin instance (informational only)",
          "_note": "Actual allowlist is enforced by daemon config",
          "allowedCommands": [
            "apt install *",
            "apt update",
            "systemctl restart *",
            "systemctl start *",
            "systemctl stop *",
            "systemctl status *"
          ]
        },

        "_unused_config_comment": "The following settings are NOT currently implemented in plugin/index.ts:",
        "_unused_config": {
          "daemonToken": "NOT_IMPLEMENTED",
          "maxRetries": "NOT_IMPLEMENTED",
          "requireReason": "NOT_IMPLEMENTED",
          "notifications": "NOT_IMPLEMENTED",
          "webhookUrl": "NOT_IMPLEMENTED",
          "webhookSecret": "NOT_IMPLEMENTED"
        },

        "_env_comment": "Environment variables for the plugin",
        "env": {
          "_comment": "Override daemon URL via environment if needed",
          "KINGCRAB_DAEMON_URL": "http://localhost:8080",
          "_comment2": "Override daemon token via environment if needed",
          "KINGCRAB_DAEMON_TOKEN": "kc_plugin_token_change_me_in_production"
        },

        "_skills_comment": "Skill-specific configuration",
        "skills": {
          "entries": {
            "kingcrab": {
              "_enabled_comment": "Enable the kingcrab skill",
              "enabled": true,

              "_config_comment": "Skill-specific configuration overrides",
              "config": {
                "_defaultReason_comment": "Default reason template if not provided",
                "defaultReasonTemplate": "Automated request from {{.Agent}}",

                "_maxReasonLength": "Maximum reason length",
                "maxReasonLength": 500,

                "_showResults_comment": "Show command execution results in chat",
                "showResults": true,

                "_truncateOutput_comment": "Truncate output to this many characters",
                "maxOutputLength": 2000
              }
            }
          }
        }
      }
    }
  },

  "_channels_comment": "Channel-specific configuration",
  "channels": {
    "telegram": {
      "_comment": "KingCrab can send inline buttons for approval",
      "capabilities": {
        "_inlineButtons_comment": "Enable inline buttons for KingCrab requests",
        "inlineButtons": "dm"
      }
    }
  },

  "_logging_comment": "Logging configuration for KingCrab plugin",
  "logging": {
    "_level_comment": "Log level: trace, debug, info, warn, error",
    "level": "info",
    "_file_comment": "Optional log file path",
    "file": ""
  }
}