{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "KingCrab Plugin Configuration",
  "description": "Configuration for the KingCrab PAM plugin - chat-based sudo approval workflows",
  "type": "object",
  "properties": {
    "daemonUrl": {
      "type": "string",
      "format": "uri",
      "default": "http://localhost:8080",
      "description": "URL of the KingCrab daemon HTTP API"
    },
    "allowedCommands": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [
        "apt install *",
        "apt update",
        "systemctl restart *",
        "systemctl start *",
        "systemctl stop *",
        "systemctl status *"
      ],
      "description": "List of allowed command patterns. Wildcards (*) are supported using regex matching."
    }
  },
  "required": [],
  "additionalProperties": false
}
